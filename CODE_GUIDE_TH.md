# MAGIC COFFEE — คู่มืออธิบายโค้ดสำหรับพรีเซนต์

ไฟล์นี้อธิบายโครงสร้างของโปรเจกต์แบบสั้น ๆ เพื่อใช้ตอบคำถามอาจารย์ โดยไม่ต้องไล่ดูทุกบรรทัดของโค้ด

## ภาพรวมการทำงาน

```text
ลูกค้าเลือกเมนู
  → Cart (ตะกร้า)
  → Checkout (ตรวจข้อมูลและเบอร์โทร)
  → Order Service (ตัด stock / คิดราคา / ใช้แต้ม)
  → Order (บันทึกคำสั่งซื้อ)
  → Admin Live Orders (ร้านรับออเดอร์และเปลี่ยนสถานะ)
  → My Order (ลูกค้าเห็นสถานะใหม่อัตโนมัติ)
```

## แบ่งหน้าที่ของโค้ด

| ส่วน | ไฟล์หลัก | หน้าที่ |
|---|---|---|
| หน้าเมนู | `apps/menu/views.py` | แสดงเมนู, ค้นหา, กรอง, เปิดรายละเอียดสินค้า |
| ตะกร้า | `apps/cart/views.py`, `apps/cart/services.py` | เพิ่ม/ลบ/เปลี่ยนจำนวนสินค้า และตรวจว่าไม่เกิน stock |
| สั่งซื้อ | `apps/orders/views.py`, `apps/orders/services.py` | รับข้อมูล checkout และส่งต่อกติกาสำคัญไปที่ service |
| ออเดอร์และแต้ม | `apps/orders/models.py` | เก็บออเดอร์, รายการในออเดอร์ และแต้มจากเบอร์โทร |
| แอดมิน | `apps/dashboard/views.py` | จัดการเมนู, ผู้ใช้, สถานะออเดอร์ และแจ้งเตือน stock |
| หน้าตา/ความ realtime | `templates/`, `static/js/site.js` | แสดงผล, เปลี่ยนจำนวนในตะกร้า และ polling สถานะโดยไม่รีเฟรช |

## จุดที่อาจารย์มักถาม

### 1. ทำไมแยก `views.py` กับ `services.py`

- `views.py` รับ request, ตรวจ form และเลือกหน้าที่จะแสดง
- `services.py` รวมกติกาธุรกิจที่สำคัญ เช่น ตัด stock, สร้างออเดอร์, ใช้แต้ม และเปลี่ยนสถานะ
- ผลคือกติกาไม่กระจายหลายหน้า และทดสอบได้ง่าย

### 2. ป้องกันขายเกิน stock อย่างไร

ใน `place_order()` ระบบใช้ database transaction และลด stock แบบมีเงื่อนไข หาก stock ไม่พอ ระบบจะไม่สร้างออเดอร์และจะย้อนกลับทุกอย่าง รวมถึงแต้มที่กำลังจะใช้

### 3. แต้มสะสมทำงานอย่างไร

- ใช้เบอร์โทรเป็นรหัสของ `LoyaltyAccount`
- เมื่อออเดอร์เป็น `completed` จะนับการซื้อ 1 ครั้ง
- ครบทุก 10 ครั้ง ได้กาแฟฟรี 1 แก้ว
- ตัวเลือกใช้กาแฟฟรีจะแสดงก็ต่อเมื่อมีสิทธิ์จริง
- หากยกเลิกออเดอร์ที่ใช้สิทธิ์ ระบบคืนสิทธิ์ให้

### 4. Realtime ทำอย่างไร

JavaScript ใน `static/js/site.js` เรียก endpoint เดิมเป็นระยะ (polling)

- ลูกค้า: สถานะออเดอร์ทุก 5 วินาที
- แอดมิน: Live Orders ทุก 2 วินาที
- แจ้งเตือน stock และ badge ออเดอร์ใหม่ทุก 4 วินาที

จึงไม่ต้องกดรีเฟรชหน้า แต่ยังใช้ Django และฐานข้อมูลเดิม ไม่ต้องเพิ่ม server realtime

### 5. ลำดับสถานะออเดอร์

```text
Pending → Preparing → Ready → Completed
             ↘ Cancelled
```

`apps/orders/services.py` เป็นจุดเดียวที่อนุญาตให้เปลี่ยนสถานะ เพื่อป้องกันการข้ามขั้นโดยไม่ตั้งใจ

## วิธีเดโมสั้น ๆ

1. เปิดหน้า MENU แล้วเพิ่มเครื่องดื่มเข้าตะกร้า
2. กด `+` ในตะกร้าเพื่อชี้ให้เห็นว่าราคาเปลี่ยนทันทีโดยไม่รีเฟรช
3. Checkout ด้วยเบอร์โทร และอธิบายว่าระบบตรวจแต้มจากเบอร์โทร
4. เปิด Admin > Live Orders ในอีกหน้าต่าง แล้วสั่งออเดอร์ใหม่เพื่อให้เห็นออเดอร์เด้งขึ้น
5. เปลี่ยนสถานะจาก Pending ไป Ready แล้วกลับไป My Order เพื่อให้เห็นสถานะเปลี่ยนอัตโนมัติ

## คำสั่งตรวจระบบ

```powershell
.\.venv\Scripts\python.exe manage.py test apps.dashboard.tests.CoffeeWorkflowTests --noinput
.\.venv\Scripts\python.exe manage.py check
node --check static\js\site.js
```

คำสั่งเหล่านี้ใช้ตรวจว่าโครงสร้างที่จัดใหม่ยังไม่กระทบการทำงานเดิม
